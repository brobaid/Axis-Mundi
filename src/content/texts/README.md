# Sacred text records (spec §5.4). One file per text; `primary: true` marks the one
# text a tradition is named by, and validate-content.ts asserts exactly one per
# tradition. The collection is wired and empty — records arrive from the owner's
# research and are never authored here.
